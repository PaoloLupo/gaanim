# Fixtures de los ejemplos

El builder de la documentación enlaza el contenido de esta carpeta dentro del
directorio de trabajo de cada ejemplo. Así los bloques que cargan archivos por
ruta relativa (`gaanim.toml`, `assets/…`) se ejecutan como en un proyecto real.

Mantén los archivos pequeños y, cuando se pueda, en formatos de texto.
