[package]
name = "gaanim_core"
version = "0.1.0"
edition = "2024"

[dependencies]
peniko = {workspace = true}
kurbo = {workspace = true}
glam = {workspace = true}
thiserror = {workspace = true}
serde = { version = "1.0", features = ["derive"], optional = true }

[features]
default = []
serde = ["dep:serde", "peniko/serde"]

