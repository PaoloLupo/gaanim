[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "gaanim"
version = "0.3.0"
description = "High-performance GPU-accelerated mathematical vector animation engine"
requires-python = ">=3.7"
dependencies = []

[tool.maturin]
python-source = "."
module-name = "gaanim.gaanim_core"
