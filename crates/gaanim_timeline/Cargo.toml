[package]
name = "gaanim_timeline"
version = "0.2.0"
edition = "2024"
description = "Timeline resource, event-sourced seek, interactive stops, and audio synchronization for gaanim v2"

[dependencies]
# Local workspace dependencies
gaanim_core = { path = "../gaanim_core" }
gaanim_math = { path = "../gaanim_math" }
gaanim_scene = { path = "../gaanim_scene" }
gaanim_animation = { path = "../gaanim_animation" }

# Third-party dependencies from workspace / registry
bevy = { workspace = true }
glam = { workspace = true }
kurbo = { workspace = true }
peniko = { workspace = true }
thiserror = { workspace = true }

# Timeline-specific dependencies
slotmap = { version = "1.1" }
ordered-float = { version = "5.3" }

# Serialization (conditional)
serde = { version = "1.0", features = ["derive"], optional = true }

[features]
dev-dynamic = [
    "bevy/dynamic_linking",
    "gaanim_animation/dev-dynamic",
    "gaanim_math/dev-dynamic",
    "gaanim_objects/dev-dynamic",
    "gaanim_renderer/dev-dynamic",
    "gaanim_scene/dev-dynamic",
]
default = []
serde = [
    "dep:serde",
    "gaanim_core/serde",
    "gaanim_math/serde",
    "gaanim_scene/serde",
    "gaanim_animation/serde",
    "peniko/serde",
    "kurbo/serde",
    "glam/serde",
    "slotmap/serde",
    "ordered-float/serde"
]

[dev-dependencies]
# The visual_demo example hosts a native window; the library does not.
bevy = { workspace = true, features = ["bevy_winit", "wayland"] }
gaanim_renderer = { path = "../gaanim_renderer" }
gaanim_objects = { path = "../gaanim_objects" }
