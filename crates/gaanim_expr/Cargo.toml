[package]
name = "gaanim_expr"
version = "0.1.0"
edition = "2024"
description = "Native mathematical expression trees and automatic differentiation for Gaanim"

[dependencies]
gaanim_core = { path = "../gaanim_core" }
thiserror = { workspace = true }

[features]
default = []
serde = ["gaanim_core/serde"]
