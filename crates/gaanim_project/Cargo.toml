[package]
name = "gaanim_project"
version = "0.2.0"
edition = "2024"
description = "Shared Gaanim project scaffolding, manifests, recents, and environment discovery"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
toml = { workspace = true }
directories = { workspace = true }

[dev-dependencies]
tempfile = "3"
